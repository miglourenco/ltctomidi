sounddevice>=0.4.6
numpy>=1.24.0

# python-rtmidi is OPTIONAL on Windows.
# The app falls back to the built-in WinMM API (no compiler needed).
# Uncomment the line below only if you have a C++ compiler installed:
# python-rtmidi>=1.5.5
